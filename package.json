{
  "name": "captcha-image-generator",
  "version": "0.1.0",
  "license": "MIT",
  "description": "An opinionated captcha image generator for displaying text - trying to mitigate the risk of OCR.",
  "main": "dist/index.js",
  "devDependencies": {
    "bun-types": "latest",
  },
  "peerDependencies": {
    "typescript": "^5.2.2"
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "bun build --target=node ./src/index.ts --outdir=dist --minify"
  },
  "types": "dist/index.d.ts",
  "dependencies": {
    "gm": "^1.25.0"
  }
}
